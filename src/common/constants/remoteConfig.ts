export const KeyRemoteConfig = {
  keyFeature: "key_feature",
  timeFirstLoginRating: "time_first_login_rating",
  appID: "app_id",
};

export enum AppEmitterEvents {
  FETCHED_REMOTE_CONFIG = "FETCHED_REMOTE_CONFIG",
}
